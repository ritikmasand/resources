// Calculator  :

// add
// substraction
// multiplication
// division

const add = (num1, num2) => {
  // declaring
  console.log("The sum is " + " " + (num1 + num2));
};
const substraction = (num1, num2) => {
  // declaring
  console.log("The differece is " + " " + (num1 - num2));
};
const multiplication = (num1, num2) => {
  // declaring
  console.log("The product is " + " " + (num1 * num2));
};
const division = (num1, num2) => {
  // declaring
  console.log("The division is " + " " + (num1 / num2));
};

multiplication(6, 3);
add(6, 3);
substraction(10 ,3)
division(10,5)